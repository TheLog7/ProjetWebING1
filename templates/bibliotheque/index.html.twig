{% extends 'base.html.twig' %}

{% block title %}Bibliothèque{% endblock %}

{% block body_class %}bibliotheque-page{% endblock %}

{% block body %}
    <header class="site-header">
        <h1>Découvrez notre bibliothèque</h1>
        <p>Trouvez un vaste choix de livres pour tous les goûts 📖</p>
    </header>

    <a href="{{ path('app_livre_ajout') }}" class="btn btn-primary my-3">Ajouter un livre</a>

    {% if livres is empty %}
        <p class="alert alert-info">Aucun livre disponible pour le moment.</p>
    {% else %}
        <table class="table table-striped mt-4">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Auteur</th>
                    <th>Date de publication</th>
                    <th>Genre</th>
                </tr>
            </thead>
            <tbody>
                {% for livre in livres %}
                    <tr>
                        <td>{{ livre.id }}</td>
                        <td>{{ livre.titre }}</td>
                        <td>{{ livre.prenomAuteur }} {{ livre.nomAuteur }}</td>
                        <td>{{ livre.datePublication ? livre.datePublication|date('d/m/Y') : 'Non renseignée' }}</td>
                        <td>{{ livre.genre }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}
