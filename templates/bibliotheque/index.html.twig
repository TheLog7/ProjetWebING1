{% extends 'base.html.twig' %}

{% block title %}Bibliothèque{% endblock %}

{% block body_class %}bibliotheque-page{% endblock %}

{% block body %}
    <header class="site-header text-center py-5 bg-primary text-white">
        <h1>📚 Découvrez notre bibliothèque</h1>
        <p class="lead">Trouvez un vaste choix de livres pour tous les goûts</p>
    </header>

    <div class="container mt-4">
    <form method="get" action="{{ path('app_livre_recherche') }}" class="d-flex">
    <input type="text" name="q" class="form-control me-2" placeholder="Rechercher un livre...">
    <button type="submit" class="btn btn-outline-light">🔍</button>
</form>

        {% if app.user is not null %}
            <div class="text-center mb-4">
                {% if app.user.type == 'Administration' and app.user.niveau >= 1 %}
                    <a href="{{ path('app_livre_ajout') }}" class="btn btn-success">➕ Ajouter un livre</a>
                {% elseif (app.user.type == 'Enseignant' and app.user.niveau >=2 ) or (app.user.type == 'Eleve' and app.user.niveau >= 3) %}
                    <a href="{{ path('app_livre_ajout')}}" class="btn btn-success">➕ Ajouter un livre</a>
                {% elseif (app.user.type == 'Eleve' and app.user.niveau >= 2) or (app.user.type == 'Enseignant' and app.user.niveau >= 1) %}
                   <a href="{{ path('app_livre_ajout') }}" class="btn btn-warning">💡 Suggérer un livre</a>
                {% endif %}
            </div>
        {% endif %}

        {% if livres is empty %}
            <p class="alert alert-info text-center">Aucun livre disponible pour le moment.</p>
        {% else %}
            <div class="row">
                {% for livre in livres %}
                    <div class="col-md-4 mb-4">
                        <div class="card shadow-sm border-0">
                            <div class="card-body text-center">
                                <h5 class="card-title">{{ livre.titre }}</h5>
                                <a href="{{ path('app_livre_informations', {'id': livre.id}) }}" class="btn btn-info mt-2">ℹ️ Informations</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
