{% extends 'base.html.twig' %}

{% block title %}Liste des Imprimantes{% endblock %}

{% block body_class %}imprimante-page{% endblock %}

{% block body %}
    <header class="site-header text-center py-5 bg-dark text-white">
        <h1>🖨️ Liste des Imprimantes</h1>
        <p class="lead">Gérez et consultez les imprimantes disponibles</p>
    </header>

    <div class="container mt-4">
        <div class="d-flex justify-content-between mb-3">
                <header class="site-header">
            <h2>Imprimantes enregistrées</h2>

            </header>
            {% if app.user is not null and (app.user.type == 'Administration' or app.user.niveau >= 2) %}
                <a href="{{ path('app_imprimante_ajout') }}" class="btn btn-success">➕ Ajouter une imprimante</a>
            {% endif %}

        </div>

        {% if imprimantes is empty %}
            <p class="alert alert-warning text-center">Aucune imprimante enregistrée pour le moment.</p>
        {% else %}
            <div class="row">
                {% for imprimante in imprimantes %}
                    <div class="col-md-4 mb-4">
                        <div class="card shadow-sm border-0">
                            <div class="card-body text-center">
                                <h5 class="card-title">{{ imprimante.nom }}</h5>
                                <p class="card-text">Modèle: {{ imprimante.modele }}</p>
                                <p class="card-text">Statut: <strong>{{ imprimante.statut }}</strong></p>
                                <p class="card-text">Niveau d'Encre: <strong>{{ imprimante.niveauEncre ? imprimante.niveauEncre ~ '%' : 'Non spécifié' }}</strong></p>
                                <p class="card-text">Niveau de Batterie: <strong>{{ imprimante.niveauBatterie ? imprimante.niveauBatterie ~ '%' : 'Non spécifié' }}</strong></p>
                                <p class="card-text">Salle: {{ imprimante.salle }}</p>
                                <a href="{{ path('app_imprimante_details', {'id': imprimante.id}) }}" class="btn btn-info mt-2">ℹ️ Détails</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
